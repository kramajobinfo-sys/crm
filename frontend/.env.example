# Relative path -> the browser calls the same host it loaded the app from
# (e.g. http://192.168.1.50:8080/api/v1) and Vite proxies it to the API.
# No need to hardcode an IP here.
VITE_API_BASE_URL=/api/v1
VITE_APP_NAME="Krama CRM"
